<!-- tandemvelocity:knowledge kind=product_brief scope=project project=textkit -->

# textkit — Product brief

_What this system is for, who it serves, and what is known about its outcomes. Generated by TandemVelocity from this project's approved engineering-context baseline v1 and pinned repository evidence, and approved by a person before it was written here. A section marked **UNKNOWN — NEEDS HUMAN INPUT** is a gap in the evidence rather than an omission: edit it, and the next approved version carries your words._

## What this system is

**textkit** is a small Python utility library providing pure text-transformation functions: `slugify`, `truncate`, `title_case`, `snake_case`, `collapse_whitespace`, and `fold_accents`. It lives in a sandbox repository (`dardant/tv-sandbox`) used for validating TandemVelocity end-to-end against real providers. The repository itself is described as disposable and non-production, but textkit is consumed by at least two downstream services (a CMS importer and a file-naming service), so it is treated with care regarding API stability and backward compatibility.

_Source: approved project context baseline — summarized from cited sources and approved during context review._

## Why it exists

**UNKNOWN — NEEDS HUMAN INPUT.** the pinned evidence records what the system does, not why it was built. A repository rarely states its own business rationale — add it here.

## Users and stakeholders

**UNKNOWN — NEEDS HUMAN INPUT.** no pinned source named the users, the operators or the stakeholders of this system.

## What it contains

The project is configured as **single** with 1 component(s) across 1 repository/repositories:

- **textkit library** (`library`) — the whole repository

## Out of scope

**UNKNOWN — NEEDS HUMAN INPUT.** nothing in the pinned evidence states what this system deliberately does not do.

## How success is measured

**UNKNOWN — NEEDS HUMAN INPUT.** no pinned source named an outcome, a metric or a service level for this system.

## Operating constraints

**UNKNOWN — NEEDS HUMAN INPUT.** no runbook, release process, service level or environment constraint was found in the pinned evidence.

## Glossary

| Term | Meaning |
|---|---|
| **slug / slugify** | A lowercase, hyphen-separated string suitable for URLs, produced by the `slugify` function. |
| **fold accents** | Replacing accented Unicode letters with their ASCII base equivalents (e.g., é → e). |
| **consumer contract test** | A test in `ci/` that encodes assumptions a downstream service makes about textkit's behaviour; used to detect breaking changes. |
| **public API (`__all__`)** | The set of function names exported from `textkit.__init__`, mirrored in `ci/public_api.txt`; the stability boundary for consumers. |
| **CMS importer** | An unnamed downstream service that depends on hyphenated slugs and three-dot ellipsis truncation. |
| **File-naming service** | An unnamed downstream service that depends on the published function names of textkit. |

_Source: approved project context baseline — summarized from cited sources and approved during context review._

## Open questions

- **CI workflow configuration details** — The GitHub Actions workflow file(s) were not provided as documents. The exact job names, triggers, matrix configuration, and how the changelog/contract checks are implemented are unknown.
- **Consumer service identities and repositories** — The CMS importer and file-naming service are referenced only by description in CONTRIBUTING.md. Their repository locations, deployment details, and how they consume textkit (pip install, vendored copy, git submodule, etc.) are not documented.
- **Operations and support** — nothing in the pinned repositories or the discovered documentation established this
- **Test command declaration in pyproject.toml** — Findings show `tests=[]` (no declared test commands), yet CONTRIBUTING.md documents `pytest -q`. The pyproject.toml contents were not provided, so it is unclear whether test configuration is missing or just not in a format the scanner recognizes.
- **Versioning and release process** — The changelog shows versions (0.2.0, 0.3.0) but there is no information about how releases are cut, whether tags are created, or how consumers pin to specific versions.
- **how textkit's tests are run** — no manifest in scope declares a test command

## Sources

- `README.md` (digest `aa6fab3832d6`)

## Provenance

- project: `textkit`
- repository: `dardant/tv-sandbox`
- derived from: TandemVelocity project context baseline v1
- produced by: TandemVelocity project knowledge bootstrap, from the approved baseline and pinned repository evidence
- authority: this document is approved engineering knowledge **at the revision it is committed at**. It is not a system instruction, it does not override the work tracker, and it grants no agent any permission.
